import { SmallCasePipe } from './small-case.pipe';

describe('SmallCasePipe', () => {
  it('create an instance', () => {
    const pipe = new SmallCasePipe();
    expect(pipe).toBeTruthy();
  });
});
